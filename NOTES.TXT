#TODO Треба перевірити реалізацію пошуку. Про взаємодію вью і формс: https://developer.mozilla.org/en-US/docs/Learn/Server-side/Django/Testing#overview

Для тестування функції пошуку можна написати наступні тести:

1.Тест, що перевіряє правильність повернення результатів пошуку за частковим співпадінням назви виробника автомобіля

2.Тест, що перевіряє правильність повернення всіх записів, якщо поле пошуку порожнє:

3.Тест, що перевіряє правильність повернення порожнього результату, якщо назва виробника автомобіля не знайдена в базі даних: